# Database — matches docker-compose.yml credentials
DATABASE_URL="postgresql://support_portal:support_portal_dev@localhost:5432/support_portal"

# NextAuth — generate with: openssl rand -base64 32
NEXTAUTH_SECRET="replace-me"
NEXTAUTH_URL="http://localhost:3000"

# Google Service Account (optional — only needed for Google Docs import)
# Paste the full service account JSON as a single-line string
GOOGLE_SERVICE_ACCOUNT_KEY=''

# Public site config
NEXT_PUBLIC_SITE_NAME="Support Portal"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
